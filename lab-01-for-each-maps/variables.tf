# TODO (Task 1): Define a variable named "buckets"
# Type: map(object({ environment = string }))
# Default: at least two entries, e.g.:
#   {
#     "logs"   = { environment = "production" }
#     "assets" = { environment = "staging" }
#   }
